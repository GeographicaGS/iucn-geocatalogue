'use strict';

App.Model.LayerModel = Backbone.Model.extend({
	url: App.config.API_URL + '/layer/',

    initialize: function(models, options) {

    },

    // parse: function(response){
    //     return response;
    // }


});
